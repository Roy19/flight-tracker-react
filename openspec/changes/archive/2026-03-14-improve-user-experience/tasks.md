## 1. Core Styling Foundation

- [x] 1.1 Update `index.css` with a refined color palette (vibrant gradients, semantic text colors).
- [x] 1.2 Add `.glass-panel` UI utility class to `index.css` leveraging `backdrop-filter` for the glassmorphism effect.
- [x] 1.3 Define CSS `@keyframes` in `index.css` for panel entry animations and loading pulses.

## 2. Component Updates

- [x] 2.1 Refactor `Sidebar.tsx` to utilize the new `.glass-panel` and text classes.
## 2. Component Updates

- [x] 2.1 Refactor `Sidebar.tsx` to utilize the new `.glass-panel` and text classes.
- [x] 2.2 Refactor `FlightDetails.tsx` to utilize the new `.glass-panel` and update layout/spacing.

## 3. Interaction & Polish

- [x] 3.1 Implement hover state transitions for interactive elements (close buttons, panel sections) within components.
- [x] 3.2 Ensure the `FlightDetails` panel smoothly animates in and out when `selectedFlight` changes.
- [x] 3.3 Verify typography weight, color contrast, and padding adjustments are visually pleasing over the map.
